def no_repeat(s):
    for letter in s:
        if s.count(letter)!=1:
            return 0
    return 1
text=input('Введите текст')
text=text.split()
answer=[]
for word in text[1:]:
    if word != text[0] and no_repeat(word)==1:
        answer.append(word)
print(answer)
